function App() {
  return (
    <div>
          <h1>hello world!</h1>
          <p>we made a react project using `npx create-react-app` command on terminal.</p>
          <p>but the traditional method can be quite slow so we also learnt to use vite library</p>
          <p>`npm create vite@latest` creates a basic structure then do `npm install`</p>
          <p>um what else, we learnt about package.json file</p>
          <p>then we deleted some random test, css etc files to clear up space and confusion</p>
    </div>
  );
}

export default App;
